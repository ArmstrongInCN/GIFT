# fno3d: completed full500 evidence

Execution: complete formal 500 epochs / 50000 updates. Integrity: PASS. Historical training losses: BITEXACT; published state: BITEXACT.

All 1,000 scalar comparisons (500 epochs x two original-unit losses), all own-export state/normalizer comparisons and every requested published-reference state/normalizer comparison remain in verification.json. No new tolerance is introduced. DIFFERENT_NOT_BITEXACT is not a numeric pass; NOT_COMPARED is not inferred agreement.

Metadata erratum: the pinned auditor mistakenly used a UNO /20 unit note for FNO. The portable unit note is corrected and its original text retained in metadata_errata; all numerical rows, summaries and statuses are unchanged. The original SHA-bound audit is not modified.

Actual same-own-run continuation was checked by the original CPU terminal audit; uninterrupted-vs-resumed full-run equality was not tested. Recorded duration is the final invocation only, not total campaign time.

This package is derived solely from the explicitly SHA-bound completed audit JSON, without reading weights/data, re-running its checks, importing models or using a GPU. Source/input hashes are preserved evidence, not newly revalidated files. M2/M3 acceptance and whole-project reproduction remain separate. No original artifact/catalogue is replaced.

完整500轮终态完整性与原报告数值复现分开：差异全部保留，不修改容差，不将未比较写成相同。此包不含模型/数据/机器路径；M2/M3需独立验收。
