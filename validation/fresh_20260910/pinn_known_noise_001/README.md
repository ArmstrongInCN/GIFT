# Full PINN-SR-KC noise_001: execution complete, numeric acceptance FAILED

The fresh GPU run completed with exit 0 in 3433.600419 seconds. It used untrained canonical initialization, not the preserved CPU checkpoint: 24,000 train rows / 84,000 physics rows, chunk32,768; 5,000 pre-NAdam, original L-BFGS-B limits10,000, six STRidge +1,000 joint-NAdam rounds, final mask, 20,000 post-NAdam. All31,000 NAdam updates and six STRidge rounds completed. No post-L-BFGS was added.

The real terminal readout yields three M1 subset rows /12 numeric comparisons, with two FAILs under the unchanged 0.0005 +0.05*abs(reference) rule: gamma absolute_error0.21602565050125122 vs0.23254746198654175 (allowed difference0.012127373099327089), and relative_error_percent21.602565050125122 vs23.254746198654175 (allowed1.1632373099327087). Gamma estimate0.7839743494987488 vs0.7674525380134583 passes its own check; being closer to the true value does not exempt the error-column failures. Candidate and reference CSVs remain separate original bytes.

Read-only saved-boundary evidence: after5,000 pre-NAdam updates, all60 primary model/optimizer/mask tensors plus22 diagnostic accumulator/total tensors are exact (82/82). After pre-L-BFGS,41/60 primary tensors remain exact: all18 network tensors and the coefficient tensor differ; optimizer slots/beta/mask remain exact. The historical L-BFGS summary has1,046 iterations/1,075 evaluations/loss1.0370837762271474; the candidate has1,356 iterations/1,423 evaluations/loss0.9482338565391261. Both report the same relative-reduction convergence condition. This locates a divergence across that phase, not its first operation or an established optimizer bug.

See [numeric FAIL](comparison.json), [execution/source/input provenance](provenance.json), [boundary summaries](boundary_comparisons.json), [actual metrics](metrics.csv), and [reference subset](reference_subset.csv). No checkpoint arrays, weights, third-party source, device UUID or private absolute paths are distributed here. Packaging performed no model import, forward, gradient or scientific recomputation. This is neither full M1 nor project-wide acceptance; open mode is not validated by this known/KC result.
