# Source, reference tables and evidence are bound by byte-level hashes.
# Disable newline conversion for every tracked file on every platform.
# Keep the exact source and evidence bytes across checkouts.
* -text
